@{
    ViewBag.Title = "لیست پکیج های یوزمنیجر";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var counter = 1;
}
<section id="configuration">
    <div class="row">
        <div class="col-xs-12">
            <div class="card">
                <div class="card-header">
                    <a value="افزودن بسته" class="btn btn-social btn-instagram" href="@Url.Action(MVC.Company.UserManager.PackageCreate())">
                        <span class="icon-plus4 font-medium-4"></span>
                        افزودن بسته
                    </a>
                    <a value="چاپ لیست" class="btn btn-social btn-instagram" href="@Url.Action(MVC.Company.UserManager.ResetCounter())">
                        <span class="icon-print font-medium-4"></span>
                        چاپ لیست
                    </a>
                </div>
                <div class="card-body collapse in">
                    <div class="card-block card-dashboard">
                        <table class="table table-striped table-bordered table-hover zero-configuration dataex-colvis-restore" id="grid">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th >نام پکیج</th>
                                    <th>قیمت</th>
                                    <th>اعتبار زمانی</th>
                                    <th>نام گروه</th>
                                    <th width="10%"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in ViewBag.userlist)
            {
                                    <tr>
                                        <td>@(counter++)</td>
                                        <td>@item.name</td>
                                        <td>@item.price</td>
                                        <td>@item.validity</td>
                                        <td>@item.name_for_users</td>
                                        <td>
                                            <span class="dropdown">
                                                <button id="btnSearchDrop2" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true" class="btn btn-outline-secondary dropdown-toggle dropdown-menu-right"><i class="icon-cog3"></i> پردازش</button>
                                                <span aria-labelledby="btnSearchDrop2" class="dropdown-menu mt-1 dropdown-menu-right">
                                                    <a onclick="PackageDetails('@item.id');" class="dropdown-item">
                                                        <i class="fa fa-book"></i> جزئیات
                                                    </a>
                                                    <a onclick="ProfileRemove('@item.id');" class="dropdown-item">
                                                        <i class="icon-trash4 danger"></i> حذف
                                                    </a>
                                                </span>
                                            </span>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
@section ScriptUp{
    <link rel="stylesheet" type="text/css" href="~/Content/css/plugins/tables/extensions/dataTables.colVis.css">
}
@section DownScript{
    <script src="~/Content/js/plugins/tables/datatable/dataTables.colVis.js" type="text/javascript"></script>
    <script type="text/javascript">
        $(function () {
            var table = $('#grid').dataTable({
                "order": [[0, 'asc']],
                "iDisplayLength": 10,
                "bProcessing": true,
                "bStateSave": true,
                "bFilter": true,
                "scrollX": true,
                "bAutoWidth": true,
                "aoColumnDefs": [{ "bSortable": false, "aTargets": [-1] }],
                "aLengthMenu": [[10, 25, 50, 100], [10, 25, 50, 100]],
                "oLanguage": {
                    "sUrl": "/Content/js/plugins/tables/datatable/dataTables.persian.txt"
                }
            });
        });


        function ProfileRemove(id) {
            PostData("@Url.Action(MVC.Company.UserManager.ProfileRemove())", { id: id });
        }

        function PackageDetails(id) {
            PostData("@Url.Action(MVC.Company.UserManager.PackageDetails())", { id: id });
        }


    </script>
}