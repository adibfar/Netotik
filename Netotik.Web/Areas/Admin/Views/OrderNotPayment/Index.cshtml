@model MvcPaging.IPagedList<Netotik.ViewModels.Common.Order.TableOrderModel>
@{
    ViewBag.Title = "فاکتورهای پرداخت نشده";
    var canAdd = User.Roles.Any(x => x == Netotik.ViewModels.Common.Security.AssignableToRolePermissions.CanCreateProduct);
}

<div class="col-lg-12" id="ContentPage">
    <section class="panel">
        <header class="panel-heading">
            @ViewBag.Title

        </header>
        <div class="panel-body">
            <form class="form-inline" role="form" action="@Url.Action(MVC.Admin.OrderNotPayment.ActionNames.Index,MVC.Admin.OrderNotPayment.Name)">
                <div class="input-group m-bot15">
                    <span class="input-group-btn">
                        <button class="btn btn-primary" type="submit">جستجو</button>
                    </span>
                    <input type="text" class="form-control" name="Search" placeholder="نام ..">
                </div>
            </form>

        </div>

        @Html.Partial("_Table", Model)

    </section>
</div>
