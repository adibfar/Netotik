@model List<Netotik.Domain.Entity.Link>
@{
    ViewBag.Title = "مدیریت پیوند ها";
}

<div class="col-lg-12" id="ContentPage">
    <section class="panel">
        <header class="panel-heading">
            @ViewBag.Title

            @Html.ActionLink("پیوند جدید", MVC.Admin.Link.ActionNames.Create, MVC.Admin.Link.Name, null, new { @class = "btn btn-sm btn-primary pull-left" })

        </header>

        @Html.Partial("_Table", Model)

    </section>

</div>

