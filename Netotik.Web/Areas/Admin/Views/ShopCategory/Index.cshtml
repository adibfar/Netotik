@model MvcPaging.IPagedList<Netotik.ViewModels.Shop.Category.TableCategoryModel>
@{
    ViewBag.Title = "مدیریت دسته بندی ها";
    var roles = Html.CurrentUserPermissions();
    var canAdd = roles.Any(x => x == Netotik.ViewModels.Identity.Security.AssignableToRolePermissions.CanCreateProductCategory);
}

<div class="col-lg-12" id="ContentPage">
    <section class="panel">
        <header class="panel-heading">
            @ViewBag.Title

            @if (canAdd)
            {
                @Html.ActionLink("دسته جدید", MVC.Admin.ShopCategory.ActionNames.Create, MVC.Admin.ShopCategory.Name, null, new { @class = "btn btn-sm btn-primary pull-left" })
            }
        </header>
        <div class="panel-body">
            <form class="form-inline" role="form" action="@Url.Action(MVC.Admin.ShopCategory.ActionNames.Index, MVC.Admin.ShopCategory.Name)">
                <div class="input-group m-bot15">
                    <span class="input-group-btn">
                        <button class="btn btn-primary" type="submit">جستجو</button>
                    </span>
                    <input type="text" class="form-control" name="Search" placeholder="نام ..">
                </div>
            </form>

        </div>

        @Html.Partial("_Table", Model)

    </section>
    <script type="text/javascript">

        function beginPaging(args) {
            // Animate
            $('#gridlist').fadeOut('normal');
        }

        function successPaging() {
            // Animate
            $('#gridlist').fadeIn('normal');
        }

        function failurePaging() {
            alert("Could not retrieve list.");
        }


    </script>



</div>
