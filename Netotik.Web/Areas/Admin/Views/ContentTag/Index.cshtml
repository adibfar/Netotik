@model List<Netotik.Domain.Entity.ContentTag>
@{
    ViewBag.Title = "مدیریت برچسب ها";
    var canAdd = User.Roles.Any(x => x == Netotik.ViewModels.Common.Security.AssignableToRolePermissions.CanCreateTag);
}

<div class="col-lg-12" id="ContentPage">
    <section class="panel">
        <header class="panel-heading">
            @ViewBag.Title
            @if (canAdd)
            {
                @Html.ActionLink("برچسب جدید", MVC.Admin.ContentTag.ActionNames.Create, MVC.Admin.ContentTag.Name, null, new { @class = "btn btn-sm btn-primary pull-left" })
            }

        </header>
        @Html.Partial("_Table", Model)
    </section>
</div>
